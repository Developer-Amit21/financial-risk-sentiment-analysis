# Force GPU-enabled PyTorch wheels (CUDA 12.1).
# If you need a different CUDA build, change `cu121` to e.g. `cu118` or `cu124`.
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

# Core
numpy
pandas
scipy
tqdm
joblib
matplotlib^C
streamlit
gdown

# ML / classic
scikit-learn
xgboost

# NLP
nltk
spacy
textblob
easygoogletranslate
fuzzywuzzy
python-Levenshtein

# Topic modeling / embeddings
transformers
sentence-transformers
bertopic
umap-learn
hdbscan

# Data source helper (used in the notebook)
kagglehub

# GPU (PyTorch) - pulled from the CUDA wheel index above (not CPU).
torch
torchvision
torchaudio

# Optional (uncomment if you actually use TF in your environment)
tensorflow
huggingface_hub
